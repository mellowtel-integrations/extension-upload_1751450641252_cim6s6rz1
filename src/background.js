// Mellowtel background script

// Original extension didn't have a background script, so we're only adding Mellowtel functionality

chrome.runtime.onInstalled.addListener(() => {
  // Mellowtel initialization code here
});

// Add any necessary Mellowtel background script logic